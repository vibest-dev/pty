{
  "name": "@vibest/pty-daemon-e2e",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "bun run test:daemon && bun run test:sdk && bun run test:flow-control",
    "test:e2e": "bun run test",
    "test:daemon": "bun test e2e.test.ts",
    "test:sdk": "bunx vitest --run layout.test.ts postinstall.test.ts",
    "test:flow-control": "bunx vitest --run flow-control.integration.test.ts"
  },
  "dependencies": {
    "@vibest/pty-daemon": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^4.0.18"
  }
}
